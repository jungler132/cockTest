import { colors } from '../../../services/config/colors'

export const styles = {
    mainViewStyle:{
        flex:1,
        backgroundColor:colors.silver
    }, 
    textStyle:{
        fontSize:20
    }
}