import { colors } from "../../services/config/colors";

export const styles = {
    imageBacgroundStyle:{
        width:180,
        height:120,
        justifyContent:'flex-end',
    }, 
    textStyle:{
        marginStart:15,
        marginBottom:15,
        fontSize:25,
        color:colors.white
    }
}