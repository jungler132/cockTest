import { colors } from '../../services/config/colors'

export const styles = {
    mainViewStyle:{
        flex:1,
        backgroundColor:colors.orange,
        justifyContent:'center',
        alignItems:'center'
    }, 
    textStyle:{
        fontSize:50,
        color:colors.humbrol,
    }
}